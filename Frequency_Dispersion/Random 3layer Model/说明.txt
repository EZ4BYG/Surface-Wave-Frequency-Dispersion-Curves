三层模型存在一个疑问导致我无法确定运行结果是否正确！
疑问：在两层模型时，深度h=[h1]只是一个1x1的矩阵，其实就是一个单独的数！
          一个数可以和任意维度的矩阵做乘法；
      但是当为三层模型时，深度h=[h1,h2]变成了一个1x2的矩阵，必须当成矩阵看待！
          此时它就不能和那些参数做矩阵乘法了(维度不符合！)，
          在这里我实在是不确定三层模型该如何拓展！